# 企业签署任务发布

`POST /api/enterprise/esign/envelope-release` 在发送签署任务前检查文件摘要、签署人身份、签名证书、签署意愿、印章授权、可信时间戳、证据留存和回调配置。

- `RELEASE`：法律与技术证据链完整，可以发送签署任务。
- `REVIEW`：不存在硬性阻断，但仍需配置签署顺序、留存策略或结果回调。
- `BLOCKED`：文件、身份、证书、意愿、印章、时间戳或审计控制失败。

接口可作为签署服务商适配器前的统一门禁，避免外部发送动作绕过企业控制。
